import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  logo: {
    flex: 1,
    resizeMode: 'contain',
  },

  s: {
    maxHeight: 80,
  },

  m: {
    maxHeight: 100,
  },

  l: {
    maxHeight: 140,
  },
})

export default styles
